import React, { useState, useEffect } from 'react';
import { 
  Camera, 
  ShieldCheck, 
  ChefHat, 
  MapPin, 
  Video,
  ShoppingBag,
  CheckCircle2,
  Image as ImageIcon,
  PlayCircle,
  PackageCheck,
  Eye
} from 'lucide-react';

const App = () => {
  return (
    <div className="min-h-screen bg-[#FCFAF2] font-sans text-[#3E2312] selection:bg-[#E5B143]/30 overflow-x-hidden">
      <Navbar />
      <HeroSection />
      <InteractiveProcessSection />
      <StreamSimulatorSection />
      <IngredientSourcingSection />
      <FeaturesSection />
      <ChefSection />
      <Footer />
    </div>
  );
};

// --- Reusable Components ---

// A styled placeholder component guiding you on what imagery to put here
const ImagePlaceholder = ({ instruction, className = "", icon = <ImageIcon className="w-8 h-8" /> }) => (
  <div className={`flex flex-col items-center justify-center p-6 text-center border-2 border-dashed border-[#D35400]/40 bg-[#E5B143]/10 transition-all hover:bg-[#E5B143]/20 group ${className}`}>
    <div className="text-[#D35400] mb-3 opacity-60 group-hover:opacity-100 group-hover:scale-110 transition-all duration-300">
      {icon}
    </div>
    <p className="text-sm font-bold text-[#6D4C41] mb-1">Upload Image Here</p>
    <p className="text-xs font-medium text-[#8D6E63] leading-snug max-w-[250px]">"{instruction}"</p>
  </div>
);

// --- Sections ---

const Navbar = () => (
  <nav className="fixed w-full z-50 bg-[#2D1B12]/95 backdrop-blur-md border-b border-[#4A2E1B] transition-all duration-300 shadow-md">
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div className="flex justify-between items-center h-20">
        <div className="flex-shrink-0 flex items-center gap-3 cursor-pointer">
          <div className="w-14 h-14 bg-[#FCFAF2] rounded-full flex items-center justify-center overflow-hidden shadow-inner border-2 border-[#E5B143]">
            <img 
              src="Round Logo BG.png" 
              alt="Open Kitchens Logo" 
              className="w-full h-full object-cover"
              onError={(e) => {
                e.target.onerror = null; 
                e.target.src = "https://images.unsplash.com/photo-1596797038530-2c107229654b?ixlib=rb-1.2.1&auto=format&fit=crop&w=100&q=80";
              }}
            />
          </div>
          <div className="flex flex-col">
            <span className="text-[#FCFAF2] font-serif font-bold text-2xl leading-none tracking-wide">Open Kitchens</span>
            <span className="text-[#E5B143] text-[10px] font-bold uppercase tracking-[0.2em] mt-1">See What You Eat</span>
          </div>
        </div>

        <div className="hidden md:flex space-x-8">
          <a href="#" className="text-[#D7CCC8] hover:text-[#E5B143] transition-colors text-sm font-medium uppercase tracking-wider">Home</a>
          <a href="#" className="text-[#D7CCC8] hover:text-[#E5B143] transition-colors text-sm font-medium uppercase tracking-wider">Menu</a>
          <a href="#" className="text-[#E5B143] font-bold text-sm uppercase tracking-wider border-b-2 border-[#E5B143] pb-1">Why Us</a>
        </div>

        <div className="flex items-center gap-4">
          <button className="text-[#FCFAF2] hover:text-[#E5B143] transition-colors relative">
            <ShoppingBag size={24} />
            <span className="absolute -top-1 -right-2 bg-[#D35400] text-white text-[10px] font-bold w-4 h-4 rounded-full flex items-center justify-center shadow-sm">0</span>
          </button>
        </div>
      </div>
    </div>
  </nav>
);

const HeroSection = () => (
  <section className="pt-32 pb-16 px-4 max-w-7xl mx-auto flex flex-col items-center text-center">
    <div className="inline-flex items-center gap-2 bg-[#E5B143]/20 text-[#B97700] px-4 py-1.5 rounded-full text-sm font-bold uppercase tracking-wider mb-6 border border-[#E5B143]/30">
      <ShieldCheck size={16} />
      100% Transparent Kitchens
    </div>
    
    <h1 className="text-4xl md:text-6xl font-extrabold text-[#3E2312] mb-6 tracking-tight font-serif max-w-4xl leading-tight">
      We believe you deserve to know <br className="hidden md:block"/>
      <span className="text-[#D35400]">exactly how your food is made.</span>
    </h1>
    
    <p className="text-lg md:text-xl text-[#6D4C41] mb-12 max-w-2xl mx-auto leading-relaxed">
      No hidden corners. No secret ingredients. Just authentic regional recipes cooked in spotless kitchens, broadcast live to your phone.
    </p>

    <div className="relative w-full max-w-5xl h-64 md:h-[450px] rounded-[2rem] overflow-hidden shadow-2xl border-4 border-[#E8E3D2]">
      <ImagePlaceholder 
        instruction="Wide hero video/photo: A bright, spotless commercial kitchen. A chef in a pristine uniform smiles while tossing a wok. Warm, inviting lighting."
        className="w-full h-full"
        icon={<Video className="w-10 h-10" />}
      />
      
      {/* Decorative floating elements to make hero feel alive even with placeholder */}
      <div className="absolute bottom-6 left-6 md:bottom-8 md:left-8 flex items-center gap-3">
        <div className="bg-[#D35400] text-white px-3 py-1.5 rounded-full text-xs font-bold uppercase tracking-wide flex items-center gap-2 shadow-lg">
          <div className="w-2 h-2 bg-white rounded-full animate-pulse"></div>
          Live Stream Standard
        </div>
      </div>
    </div>
  </section>
);

const InteractiveProcessSection = () => {
  const [activeStep, setActiveStep] = useState(0);

  const steps = [
    {
      title: "1. The Order Drops",
      desc: "As soon as you place your order, our kitchen receives it. You instantly get an SMS with a secure link to your dedicated camera feed.",
      icon: <ShoppingBag size={24} />,
      instruction: "Photo: A smartphone screen showing the 'Order Accepted & Live Stream Link' notification, resting on a clean wooden table."
    },
    {
      title: "2. Cameras Roll",
      desc: "Watch live as our chef gathers fresh ingredients and begins cooking your specific meal. No pre-packaged mixes, just authentic prep.",
      icon: <Video size={24} />,
      instruction: "Video Loop: Close-up of fresh vegetables being chopped or spices being tempered in hot ghee. High quality, vivid colors."
    },
    {
      title: "3. Quality Seal",
      desc: "Once plated, your food is sealed with a tamper-evident tape right in front of the camera, ensuring nobody touches it until you do.",
      icon: <PackageCheck size={24} />,
      instruction: "Photo: Chef's hands applying a branded 'Open Kitchens Tamper-Proof Seal' across the delivery container lid."
    },
    {
      title: "4. Live Tracking",
      desc: "The camera turns off, but the tracking continues. Follow your rider live on the map with 5-second GPS updates until they reach your door.",
      icon: <MapPin size={24} />,
      instruction: "Photo: A delivery rider in Open Kitchens uniform handing over the sealed package to a smiling customer."
    }
  ];

  return (
    <section className="py-24 bg-white border-t border-[#E8E3D2]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-16">
          <h2 className="text-3xl md:text-5xl font-bold font-serif text-[#3E2312] mb-4">The Transparency <span className="text-[#D35400]">Trail</span></h2>
          <p className="text-[#6D4C41] text-lg">Click through to see how we maintain visibility from our pan to your plate.</p>
        </div>

        <div className="flex flex-col md:flex-row gap-12 items-center">
          {/* Interactive Menu List */}
          <div className="w-full md:w-1/2 flex flex-col gap-4">
            {steps.map((step, idx) => (
              <div 
                key={idx}
                onClick={() => setActiveStep(idx)}
                className={`p-6 rounded-[2rem] cursor-pointer transition-all duration-300 border-2 ${
                  activeStep === idx 
                    ? "bg-[#FCFAF2] border-[#D35400] shadow-md transform scale-[1.02]" 
                    : "bg-white border-transparent hover:border-[#E8E3D2]"
                }`}
              >
                <div className="flex items-start gap-4">
                  <div className={`p-3 rounded-full mt-1 ${activeStep === idx ? "bg-[#D35400] text-white" : "bg-[#F3EFE0] text-[#8D6E63]"}`}>
                    {step.icon}
                  </div>
                  <div>
                    <h3 className={`text-xl font-bold font-serif mb-2 ${activeStep === idx ? "text-[#D35400]" : "text-[#3E2312]"}`}>
                      {step.title}
                    </h3>
                    <p className={`leading-relaxed ${activeStep === idx ? "text-[#5D4037]" : "text-[#8D6E63]"}`}>
                      {step.desc}
                    </p>
                  </div>
                </div>
              </div>
            ))}
          </div>

          {/* Dynamic Image Display */}
          <div className="w-full md:w-1/2 h-80 md:h-[500px]">
            <div className="w-full h-full rounded-[2rem] overflow-hidden shadow-xl border-4 border-[#E8E3D2] relative bg-[#FCFAF2]">
              {/* Added key to force re-render/animation on step change */}
              <div key={activeStep} className="absolute inset-0 animate-in fade-in zoom-in-95 duration-500">
                 <ImagePlaceholder 
                   instruction={steps[activeStep].instruction} 
                   className="w-full h-full rounded-none border-none"
                 />
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
};

const StreamSimulatorSection = () => {
  const [streamStage, setStreamStage] = useState(0);
  const stages = ["Prepping Ingredients...", "Cooking Litti Chokha...", "Packing Order #4092..."];
  
  useEffect(() => {
    const timer = setInterval(() => {
      setStreamStage((prev) => (prev + 1) % stages.length);
    }, 3000);
    return () => clearInterval(timer);
  }, []);

  return (
    <section className="py-24 bg-[#2D1B12] text-[#FCFAF2] relative overflow-hidden">
      {/* Background patterns */}
      <div className="absolute top-0 right-0 w-96 h-96 bg-[#D35400] opacity-5 rounded-full blur-3xl transform translate-x-1/2 -translate-y-1/2"></div>
      
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div className="flex flex-col lg:flex-row items-center gap-16">
          
          <div className="lg:w-1/2 space-y-8">
            <h2 className="text-4xl md:text-5xl font-bold leading-tight font-serif text-[#FCFAF2]">
              Try The <span className="text-[#E5B143]">Simulator</span>
            </h2>
            <p className="text-lg text-[#D7CCC8] leading-relaxed">
              Curious how it looks on your phone? Every order generates a unique, secure dashboard just like this one.
            </p>
            <ul className="space-y-5 mt-8">
              {[
                'High-definition RTSP camera feed', 
                'Zero blind-spots in the prep area', 
                'Live status updates synced to the video'
              ].map((item, i) => (
                <li key={i} className="flex items-center gap-4 text-[#FCFAF2] font-medium text-lg bg-[#3E2312] p-4 rounded-2xl shadow-sm border border-[#4A2E1B]">
                  <div className="bg-[#E5B143]/20 p-2 rounded-full text-[#E5B143]">
                    <Eye size={24} />
                  </div>
                  {item}
                </li>
              ))}
            </ul>
          </div>

          {/* Phone Simulator */}
          <div className="lg:w-1/2 flex justify-center">
            <div className="w-[320px] h-[650px] bg-black rounded-[3rem] border-[12px] border-[#4A2E1B] p-4 shadow-2xl relative overflow-hidden flex flex-col">
              <div className="absolute top-0 inset-x-0 h-6 flex justify-center">
                <div className="w-32 h-6 bg-[#4A2E1B] rounded-b-3xl"></div>
              </div>
              
              <div className="mt-8 flex justify-between items-center mb-4 px-2 text-white">
                <span className="font-bold text-sm">Demo Stream</span>
                <span className="bg-[#D35400] text-white text-[10px] px-2 py-0.5 rounded font-bold uppercase flex items-center gap-1">
                  Preview
                </span>
              </div>

              {/* Video Feed Placeholder inside phone */}
              <div className="flex-1 rounded-2xl relative overflow-hidden group border border-white/10 bg-[#1A1A1A]">
                <ImagePlaceholder 
                  instruction="Vertical video: Top-down angle showing a chef's hands assembling ingredients on a pristine steel counter."
                  className="w-full h-full rounded-2xl border-none opacity-80"
                  icon={<PlayCircle className="w-10 h-10" />}
                />
                
                <div className="absolute top-4 right-4 bg-red-600 text-white text-[10px] px-2 py-1 rounded font-bold uppercase flex items-center gap-1 shadow-lg">
                  <div className="w-1.5 h-1.5 bg-white rounded-full animate-pulse"></div> Live
                </div>

                <div className="absolute bottom-4 left-4 right-4 bg-black/80 backdrop-blur-md p-3 rounded-xl border border-white/10 flex items-center gap-3">
                   <div className="w-8 h-8 rounded-full bg-[#E5B143] flex items-center justify-center text-black">
                     <ChefHat size={16} />
                   </div>
                   <div>
                     <p className="text-[10px] text-gray-400 uppercase tracking-wider">Status</p>
                     <p className="text-sm font-bold text-white">{stages[streamStage]}</p>
                   </div>
                </div>
              </div>

              <div className="mt-4 bg-[#1A1A1A] p-4 rounded-2xl border border-white/10">
                <div className="flex justify-between items-center mb-2">
                  <span className="text-xs text-gray-400 font-bold">Driver Status</span>
                  <span className="text-xs font-black text-[#E5B143]">Waiting for prep...</span>
                </div>
                <div className="w-full bg-stone-800 h-2 rounded-full overflow-hidden">
                  <div className="bg-[#E5B143] w-1/3 h-full rounded-full animate-pulse"></div>
                </div>
              </div>
            </div>
          </div>

        </div>
      </div>
    </section>
  );
};

const IngredientSourcingSection = () => {
  const sources = [
    { title: "Pure Desi Ghee", desc: "Sourced directly from dairy farms in Punjab. No refined oils or artificial fats are ever used in our traditional mains." },
    { title: "Whole Spice Blends", desc: "We don't buy powders. We buy whole spices, roast them in-house, and grind them daily for maximum aroma." },
    { title: "Farm Fresh Produce", desc: "Vegetables arrive every morning at 5 AM. If it's not fresh enough for our families, it doesn't enter our kitchen." }
  ];

  return (
    <section className="py-24 bg-[#FCFAF2]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-16">
          <h2 className="text-3xl md:text-5xl font-bold font-serif text-[#3E2312] mb-4">Nothing to <span className="text-[#D35400]">Hide</span></h2>
          <p className="text-[#6D4C41] text-lg">Transparency isn't just about cameras. It's about ingredients.</p>
        </div>

        <div className="grid md:grid-cols-3 gap-6">
          {sources.map((item, idx) => (
            <div key={idx} className="group relative h-72 rounded-[2rem] overflow-hidden cursor-pointer shadow-md">
              <ImagePlaceholder 
                instruction={`Photo representing ${item.title} (e.g., jar of golden ghee, pile of whole spices, fresh veggies).`}
                className="absolute inset-0 w-full h-full rounded-[2rem] group-hover:scale-105 transition-transform duration-700"
              />
              {/* Overlay that darkens on hover */}
              <div className="absolute inset-0 bg-[#2D1B12]/60 group-hover:bg-[#2D1B12]/80 transition-colors duration-300"></div>
              
              <div className="absolute inset-0 p-8 flex flex-col justify-end text-[#FCFAF2]">
                <h3 className="text-2xl font-bold font-serif mb-2 transform translate-y-4 group-hover:translate-y-0 transition-transform duration-300">{item.title}</h3>
                <p className="text-[#D7CCC8] text-sm opacity-0 group-hover:opacity-100 transform translate-y-4 group-hover:translate-y-0 transition-all duration-300 delay-75">
                  {item.desc}
                </p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

const FeaturesSection = () => {
  const features = [
    {
      icon: <Camera className="w-6 h-6 text-[#D35400]" />,
      title: "Per-Order Streaming",
      desc: "Watch your specific meal being prepared. No generic footage.",
      imgReq: "Photo: Focus on a modern web camera mounted securely above a cooking station."
    },
    {
      icon: <ShieldCheck className="w-6 h-6 text-[#D35400]" />,
      title: "Spotless Hygiene",
      desc: "See the clean countertops and gloved hands yourself.",
      imgReq: "Photo: Close up of a chef's gloved hands washing vegetables in a clean steel sink."
    },
    {
      icon: <ChefHat className="w-6 h-6 text-[#D35400]" />,
      title: "Home-Style Recipes",
      desc: "Generations-old recipes perfected by tradition, not factories.",
      imgReq: "Photo: A rustic cast iron pan with sizzling spices and curry leaves."
    },
    {
      icon: <MapPin className="w-6 h-6 text-[#D35400]" />,
      title: "Live GPS Tracking",
      desc: "Track your rider live on the map from kitchen to door.",
      imgReq: "Photo: A smartphone displaying a map route, held by a customer."
    }
  ];

  return (
    <section className="py-24 bg-white border-t border-[#E8E3D2]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center max-w-3xl mx-auto mb-16">
          <h2 className="text-4xl font-bold text-[#3E2312] mb-4 font-serif">The <span className="text-[#D35400]">Open Kitchen</span> Standard</h2>
        </div>

        <div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {features.map((feature, idx) => (
            <div key={idx} className="bg-[#FCFAF2] rounded-[2rem] overflow-hidden shadow-sm border border-[#E8E3D2] hover:shadow-xl hover:-translate-y-2 transition-all duration-300 flex flex-col">
              <div className="h-40 border-b border-[#E8E3D2]">
                <ImagePlaceholder 
                  instruction={feature.imgReq}
                  className="w-full h-full rounded-none border-none bg-white"
                />
              </div>
              <div className="p-6 flex flex-col flex-grow text-center items-center">
                <div className="w-12 h-12 bg-[#F3EFE0] rounded-full flex items-center justify-center mb-4">
                  {feature.icon}
                </div>
                <h3 className="text-lg font-bold text-[#3E2312] mb-2 font-serif">{feature.title}</h3>
                <p className="text-sm text-[#6D4C41] leading-relaxed">{feature.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
};

const ChefSection = () => (
  <section className="py-24 bg-[#2D1B12] text-[#FCFAF2] relative overflow-hidden">
    <div className="absolute bottom-0 left-0 w-80 h-80 bg-[#E5B143] opacity-5 rounded-full blur-3xl transform -translate-x-1/2 translate-y-1/2"></div>

    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center relative z-10">
      <h2 className="text-4xl font-bold mb-4 font-serif text-[#FCFAF2]">Meet the <span className="text-[#E5B143]">Transparency Team</span></h2>
      <p className="text-[#D7CCC8] mb-16 max-w-2xl mx-auto text-lg">The masters behind the camera, bringing authentic recipes to life.</p>
      
      <div className="grid md:grid-cols-3 gap-12 max-w-4xl mx-auto">
        {[
          { name: "Chef Rahul", role: "Master of Indori Poha", req: "Portrait: Chef looking warm and inviting, wearing a clean uniform with subtle traditional Indian accents." },
          { name: "Chef Priya", role: "Head of Hygiene & Prep", req: "Portrait: Confident female chef adjusting her apron in a spotless kitchen setting." },
          { name: "Chef Amit", role: "Traditional Breads Expert", req: "Portrait: Chef smiling while holding a rolling pin or dusting flour." }
        ].map((chef, idx) => (
          <div key={idx} className="flex flex-col items-center bg-[#3E2312] p-8 rounded-[2rem] border border-[#4A2E1B] shadow-xl hover:-translate-y-2 transition-transform duration-300">
            <div className="w-32 h-32 rounded-full overflow-hidden border-4 border-[#E5B143] mb-6 relative group shadow-inner bg-[#4A2E1B]">
               <ImagePlaceholder 
                 instruction={chef.req}
                 className="absolute inset-0 w-full h-full rounded-full border-none p-2"
               />
            </div>
            <h3 className="text-xl font-bold font-serif">{chef.name}</h3>
            <p className="text-[#D35400] text-sm mt-2 font-bold uppercase tracking-wider">{chef.role}</p>
          </div>
        ))}
      </div>
    </div>
  </section>
);

const Footer = () => (
  <footer className="bg-[#2D1B12] text-[#D7CCC8] py-12 border-t-4 border-[#D35400]">
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-col md:flex-row justify-between items-center gap-6">
      <div className="flex items-center gap-3">
        <div className="w-12 h-12 bg-[#FCFAF2] rounded-full flex items-center justify-center overflow-hidden border border-[#E5B143]">
           <img 
              src="Round Logo BG.png" 
              alt="Open Kitchens Logo" 
              className="w-full h-full object-cover"
              onError={(e) => {
                e.target.onerror = null; 
                e.target.src = "https://images.unsplash.com/photo-1596797038530-2c107229654b?ixlib=rb-1.2.1&auto=format&fit=crop&w=100&q=80";
              }}
            />
        </div>
        <div className="flex flex-col">
          <span className="text-[#FCFAF2] font-serif font-bold text-xl">Open Kitchens</span>
          <span className="text-[#E5B143] text-[10px] tracking-widest uppercase">See What You Eat</span>
        </div>
      </div>
      <p className="text-sm font-medium">© 2026 Open Kitchens. Authentic Recipes, Radical Transparency.</p>
    </div>
  </footer>
);

export default App;